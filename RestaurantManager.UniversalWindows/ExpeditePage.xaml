<Page
    x:Class="RestaurantManager.UniversalWindows.ExpeditePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RestaurantManager.UniversalWindows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:RestaurantManager.Models"
    mc:Ignorable="d">
    <Page.TopAppBar>
        <AppBar>
            <AppBarButton x:Name="Button_Home"
                          Icon="Home" Label="Home"
                          Click="Button_Home_Click"/>
        </AppBar>
    </Page.TopAppBar>
    <Page.DataContext>
        <models:DataManager />
    </Page.DataContext>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="9*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <GridView x:Name="OrderItemsGridView" ItemsSource="{Binding Path=OrderItems, Source={StaticResource DataManager}}" />
                  
        <Button x:Name="Button_ClearAllOrders"
                Content="Clear All Orders"
                Grid.Row="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Click="Button_ClearAllOrders_Click"/>
        
    </Grid>
</Page>
